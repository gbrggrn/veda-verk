@inject IBookingService BookingService

@if (Product != null)
{
    <div class="modal show d-block animate-fade-in" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-xl animate-zoom-in">
            <div class="modal-content" style="background-color: #FFFFFF">
                <div class="modal-header border-bottom-0">
                    <h5 class="modal-title card-text fs-3">Bokning</h5>
                    <button type="button" class="btn-close" @onclick="OnClose"></button>
                </div>

                <div class="modal-body pt-0">
                    <EditForm Model="bookingModel" OnValidSubmit="HandleBooking">
                        <DataAnnotationsValidator />

                        <div class="row g-4">

                            <div class="col-md-5 col-lg-4">
                                <div class="card border-0 h-100" style="background-color: #F5F3EA">
                                    @if (!string.IsNullOrEmpty(Product.ImageUrl))
                                    {
                                        <img src="@Product.ImageUrl" class="card-img-top" style="height: 150px; object-fit: cover;" alt="@Product.Name">
                                    }
                                    <div class="card-body">
                                        <h4 class="card-title card-text mb-3">@Product.Name</h4>
                                        <p class="card-text small">@Product.Description</p>

                                        <hr class="my-3" />

                                        <div class="mb-3">
                                            <small class="text-uppercase card-text" style="font-size: 0.75rem;">Valt Datum</small>
                                            <div class="fs-5 card-text">@bookingModel.BookingDate?.ToString("yyyy-MM-dd" ?? "-")</div>
                                        </div>

                                        <div class="mb-3">
                                            <small class="text-uppercase card-text" style="font-size: 0.75rem;">Vald Tid</small>
                                            <div class="fs-5 card-text @(bookingModel.BookingTime.HasValue ? "text-success" : "text-muted fst-italic")">
                                                @(bookingModel.BookingTime.HasValue? bookingModel.BookingTime.Value.ToString("HH:mm") : "-")
                                            </div>
                                        </div>

                                        <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top">
                                            <span class="card-text">Pris totalt:</span>
                                            <span class="fs-4 card-text">@Product.Price kr</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="col-md-7 col-lg-8">
                                <div class="p-2">
                                    <label class="form-label card-text fs-4 mb-3">1. Välj datum och tid</label>
                                    
                                    <div class="d-flex gap-2 overflow-auto pb-2" style="white-space: nowrap;">

                                        @foreach (var date in GetDateRange())
                                        {
                                            <button type="button"
                                                    class="btn @GetDateClass(date) border rounded-3 px-3 py-2 d-flex flex-column align-items-center"
                                                    @onclick="() => SelectDate(date)"
                                                    style="min-width: 70px;">

                                                <small class="text-uppercase card-text" style="font-size: 0.7rem;">
                                                    @date.ToString("ddd", new System.Globalization.CultureInfo("sv-SE"))
                                                </small>

                                                <span class="fs-5 fw-bold lh-1">@date.Day</span>

                                                <small class="card-text" style="font-size: 0.65rem;">
                                                    @date.ToString("MMM", new System.Globalization.CultureInfo("sv-SE"))
                                                </small>
                                            </button>
                                        }
                                    </div>

                                    <div class="mb-4">
                                        @if (availableSlots == null)
                                        {
                                            <div class="spinner-border card-text text-muted spinner-border-sm" role="status"></div>
                                            <span class="ms-2 card-text text-muted">Laddar tider...</span>
                                        }
                                        else if (!availableSlots.Any())
                                        {
                                            <div class="alert alert-warning py-2">Inga tider tillgängliga detta datum.</div>
                                        }
                                        else
                                        {
                                            <div class="d-flex flex-wrap gap-2">
                                                @foreach (var slot in availableSlots)
                                                {
                                                    <button type="button"
                                                            class="btn @GetButtonClass(slot) btn-veda-available d-flex flex-column align-items-center py-2 px-3"
                                                            style="min-width: 80px;"
                                                            disabled="@(!slot.IsAvailable)"
                                                            @onclick="() => SelectSlot(slot)">

                                                        <span class="card-text">@slot.Time.ToString(@"hh\:mm")</span>
                                                    </button>
                                                }
                                            </div>
                                        }
                                        <ValidationMessage For="@(() => bookingModel.BookingTime)" />
                                    </div>

                                    <label class="form-label card-text fs-4 mb-2 mt-2">1. Dina uppgifter</label>

                                    <div class="row g-2 mb-3">
                                        <div class="col-md-6">
                                            <label class="form-label small card-text">Namn</label>
                                            <InputText @bind-Value="bookingModel.CustomerName" class="form-control veda-input-field" placeholder="Förnamn Efternamn" />
                                            <ValidationMessage For="@(() => bookingModel.CustomerName)" />
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label small card-text">Email</label>
                                            <InputText @bind-Value="bookingModel.CustomerEmail" class="form-control veda-input-field" placeholder="namn@exempel.se" />
                                            <ValidationMessage For="@(() => bookingModel.CustomerEmail)" />
                                        </div>
                                    </div>

                                    <div class="row g-2 mb-3">
                                        <div class="col-md-6">
                                            <label class="form-label small card-text">Antal</label>
                                            <InputNumber @bind-Value="bookingModel.Quantity" class="form-control veda-input-field" min="1" max="@bookingModel.Quantity" />
                                            <ValidationMessage For="@(() => bookingModel.Quantity)" />
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label small card-text">Mobiltelefon</label>
                                            <InputText @bind-Value="bookingModel.CustomerPhone" class="form-control veda-input-field" placeholder="0707770077" />
                                            <ValidationMessage For="@(() => bookingModel.CustomerPhone)" />
                                        </div>
                                    </div>

                                    <div class="d-grid">
                                        <button type="submit" class="btn btn-veda-verk py-3 shadow-sm" disabled="@(bookingModel.BookingTime == null)">
                                            Bekräfta Bokning
                                        </button>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public ResponseProductDTO? Product { get; set; }
    [Parameter] public EventCallback OnClose { get; set; }
    private CreateBookingDTO bookingModel = new();
    private TimeSpan? SelectedTime { get; set; }
    List<TimeSlotDTO>? availableSlots;
    private DateTime selectedDate = DateTime.Today;

    protected override async Task OnParametersSetAsync()
    {
        if (Product != null)
        {
            if (bookingModel.ProductId != Product.Id)
            {
                bookingModel = new CreateBookingDTO
                    {
                        ProductId = Product.Id,
                        Quantity = 1,
                        BookingDate = selectedDate
                    };
            }

            SelectedTime = null;
            availableSlots = null;
        }
        await LoadSlots();
    }

    private async Task LoadSlots()
    {
        if (Product == null)
            return;

        availableSlots = await BookingService.GetAvailableSlotsAsync(Product.Id, selectedDate);
    }

    private void SelectDate(DateTime date)
    {
        selectedDate = date;
        bookingModel.BookingDate = date;

        SelectedTime = null;
        bookingModel.BookingTime = default;

        _ = LoadSlots();
    }

    private IEnumerable<DateTime> GetDateRange()
    {
        if (Product == null)
            yield break;

        var start = Product.ActiveFrom > DateTime.Today
            ? Product.ActiveFrom // Start from product start date
            : DateTime.Today; // Start from today

        var end = Product.ActiveTo;

        var effectiveEnd = end > start.AddDays(30) ? start.AddDays(30) : Product.ActiveTo;

        for (var date = start; date <= effectiveEnd; date = date.AddDays(1))
        {
            yield return date;
        }
    }

    private string GetDateClass(DateTime date)
    {
        if (date.Date == selectedDate.Date)
        {
            // When btn is selected
            return "btn-veda-active";
        }

        // When btn is not selected
        return "btn-veda-outline border-opacity-25";
    }

    private void SelectSlot(TimeSlotDTO slot)
    {
        if (!slot.IsAvailable)
            return;

        SelectedTime = slot.Time;
        bookingModel.BookingTime = selectedDate.Date.Add(slot.Time);
    }

    private async Task HandleBooking()
    {
        await BookingService.Create(bookingModel);

        await OnClose.InvokeAsync();
    }

    private string GetButtonClass(TimeSlotDTO slot)
    {
        if (!slot.IsAvailable) 
            return "disabled";
        if (SelectedTime == slot.Time)
            return "border-4";

        return "";
    }
}